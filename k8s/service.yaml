apiVersion: v1
kind: Service
metadata:
  name: garage-auth-service
  namespace: default
spec:
  selector:
    app: garage-auth-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
